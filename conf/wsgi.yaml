use: auto
events: 16
worker_connections: 8
servers:
- listen: unix:/tmp/sample.sock
  #application: my_site
  #client_header_buffer_size: 1024
  #runtime: python
  #script: test.app
  #environ:
  #  SCRIPT_NAME: ''
- listen: 127.0.0.1:8080
